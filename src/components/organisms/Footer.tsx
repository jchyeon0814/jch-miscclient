import styled from '@emotion/styled';

const FooterContainer = styled.div`
  display: flex;
  height: 80px;
  width: 100%;
  background-color: #124559;
`;

export const Footer = () => {
    return (
        <FooterContainer>
        </FooterContainer>
    );
};