import styled from '@emotion/styled';

const HeaderContainer = styled.div`
  display: flex;
  height: 80px;
  width: 100%;
  background-color: #3f2e3e;
`;

export const Header = () => {
    return (
        <HeaderContainer>
        </HeaderContainer>
    );
};